
<!--<login :user="<%= resource.to_json %>" :url="'<%= session_path(resource_name) %>'"></login>-->

<div class="columns">
  <div class="column is-4-desktop is-6-tablet is-12-mobile is-offset-4-desktop is-offset-3-tablet has-text-centered mt-50">
    <app-card>
      <p class="title is-3">Inicia sesión</p>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label "Correo electrónico" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'input' %>
        </div>

        <div class="field">
          <%= f.label 'Contraseña' %><br />
          <%= f.password_field :password, autocomplete: "off", class: 'input' %>
        </div>

        <% if devise_mapping.rememberable? -%>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label 'Recordarme' %>
          </div>
        <% end -%>

        <div class="actions">
          <button class="button is-link"><i class="fa fa-sign-in"></i>Iniciar sesión</button>
        </div>
      <% end %>
      <!--<%= render "devise/shared/links" %>-->
    </app-card>
  </div>
</div>
